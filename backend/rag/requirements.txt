# Phase 7 (rag/*) dependencies. All CPU-only, all free/local.
chromadb>=0.5
langchain-text-splitters>=0.2
scikit-learn>=1.3
joblib>=1.3
requests>=2.31

# Optional but recommended: enables the real BAAI/bge-small-en-v1.5
# embeddings. Without it (or without network access to download the
# model once), rag/embeddings.py automatically falls back to the
# TF-IDF backend -- ingestion/retrieval keep working either way.
sentence-transformers>=2.2

# Testing
pytest>=8.0
